import React from "react";
import { View } from "react-native";
import { Text } from "react-native-elements";

const Attendance = ({navigation}) => {
    return(
        <View>
            <Text h1>Attendance</Text>
        </View>
    )
}

export default Attendance