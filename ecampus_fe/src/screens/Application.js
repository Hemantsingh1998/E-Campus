import React from "react";
import { View } from "react-native";
import { Text } from "react-native-elements";

const Application = ({navigation}) => {
    return(
        <View>
            <Text h1>Application</Text>
        </View>
    )
}

export default Application