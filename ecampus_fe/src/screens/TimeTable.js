import React from "react";
import { View } from "react-native";
import { Text } from "react-native-elements";

const TimeTable = ({navigation}) => {
    return(
        <View>
            <Text h1>TimeTable</Text>
        </View>
    )
}

export default TimeTable